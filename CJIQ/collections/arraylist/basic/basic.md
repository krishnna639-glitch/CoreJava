
# Basic colletion program of java (arraylist)

## 1.Create an ArrayList of integers and print all elements.

* ArrayList<Integer> numbers = new ArrayList<>(); --> Create Arraylist of integer

* Add items by numbers.add(1);

* print by two way  for loop and simply print
    - System.out.print(numbers);
    - for(int num : numbers)
        print(num)

## 2.Add 10 student names to an ArrayList and display them.


* ArrayList<String> names = new ArrayList<>(); --> Create Arraylist of integer

* Add items by names.add("alice");

* print by two way  for loop and simply print
    - System.out.print(names);
    - for(String name : names)
        print(name)

## 3 Find the size of an ArrayList.

* int totalsize = names.size()
    - print(totalsize)

## 4 Retrieve an element using its index.

* String secondStudent = names.get(1);

## 5 Update an element at a specific index.

* students.set(1, "David");









